#include <stdio.h>

int main()
{
    printf("sizeof char = %lu, short = %lu, int = %lu, long = %lu, longlong = %lu, size_t = %lu\n", sizeof(char), sizeof(short), sizeof(int), sizeof(long), sizeof(long long), sizeof(size_t));
    return 0;
}
